export const metadata = {
  title: "Members | International Economic Society",
  description:
    "Your IES membership: join in a minute, then enter the GEC, the EPR and the GRP from one place.",
};

export default function MembersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
